name: Deploy Chat Service

on:
  push:
    branches: ["prod"]
    paths:
      - "services/chat-service/**"
      - "infrastructure/docker/docker-compose.services.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  run-deploy:
    uses: ./.github/workflows/deploy-template.yml
    with:
      service_folder: "chat-service"
      image_name: "chat-service"
      compose_service: "chat-service"
      env_image_var: "CHAT_SERVICE_IMAGE"
    secrets: inherit
