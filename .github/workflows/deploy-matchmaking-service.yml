name: Deploy Matchmaking Service

on:
  push:
    branches: ["prod"]
    paths:
      - "services/matchmaking-service/**"
      - "infrastructure/docker/docker-compose.services.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  run-deploy:
    uses: ./.github/workflows/deploy-template.yml
    with:
      service_folder: "matchmaking-service"
      image_name: "matchmaking-service"
      compose_service: "matchmaking-service"
      env_image_var: "MATCHMAKING_SERVICE_IMAGE"
    secrets: inherit
