name: Deploy User Service

on:
  push:
    branches: ["prod"]
    paths:
      - "services/user-service/**"
      - "infrastructure/docker/docker-compose.services.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  run-deploy:
    uses: ./.github/workflows/deploy-template.yml
    with:
      service_folder: "user-service"
      image_name: "user-service"
      compose_service: "user-service"
      env_image_var: "USER_SERVICE_IMAGE"
    secrets: inherit
