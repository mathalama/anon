# WebSocket API

WebSocket is used for chat and signaling because the connection is bidirectional
and long-lived.

Document here:

- room join and leave
- text messages
- typing events
- heartbeat
- next-match flow
- WebRTC signaling messages

Keep event names and payloads versioned if the protocol changes.
