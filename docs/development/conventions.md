# Conventions

Keep the codebase consistent with these conventions:

- keep transport, usecase, repository, and client layers separate
- keep service ownership explicit
- keep docs close to the code path they describe
- prefer small, single-purpose pages in `docs/`
