# Nginx

Nginx Proxy Manager is used as the public reverse proxy in the current setup.

Document here:

- frontend host routing
- API host routing
- WebSocket support
- TLS termination
- domain and DNS setup
