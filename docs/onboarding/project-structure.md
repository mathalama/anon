# Project Structure

Repository layout:

- `services/` for Go microservices
- `apps/web/` for the frontend
- `libs/` for shared modules
- `infrastructure/` for deployment and ops assets
- `docs/` for engineering documentation
