apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
  namespace: sumdyk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chat-service
  template:
    metadata:
      labels:
        app: chat-service
    spec:
      containers:
      - name: chat-service
        image: ghcr.io/mathalama/chat-service:latest
        ports:
        - containerPort: 8083
        envFrom:
        - configMapRef:
            name: sumdyk-config
        - secretRef:
            name: sumdyk-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 10
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: sumdyk
spec:
  selector:
    app: chat-service
  ports:
  - port: 8083
    targetPort: 8083
