apiVersion: apps/v1
kind: Deployment
metadata:
  name: moderation-service
  namespace: sumdyk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: moderation-service
  template:
    metadata:
      labels:
        app: moderation-service
    spec:
      containers:
      - name: moderation-service
        image: __IMAGE__
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
        ports:
        - containerPort: 8084
        env:
        - name: PORT
          value: "8084"
        - name: DB_URL
          valueFrom:
            secretKeyRef:
              name: sumdyk-secrets
              key: MODERATION_DB_URL
        envFrom:
        - configMapRef:
            name: sumdyk-config
        - secretRef:
            name: sumdyk-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 10
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: moderation-service
  namespace: sumdyk
spec:
  selector:
    app: moderation-service
  ports:
  - port: 8084
    targetPort: 8084
